<!DOCTYPE html>
<html lang="en">
<head>
  <title>Stocklist</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body ng-app="stocklist">
 <div ng-controller="NavCtrl" class="navbar">
  <div class="navbar-inner">
    <a class="brand" href="<%= url_for(:action => 'index') %>">Stocklist</a>
    <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </a>
    <div class="nav-collapse collapse">
      <ul class="nav">
        <li ng-class="{active: havePath('/home')}">
           <a href="<%= url_for(:action => 'index', :anchor => '/home') %>">Home</a>
        </li>
        <li ng-show="currentUser != null" ng-class="{active: havePath('/stocklist')}" class="dropdown">
           <a href class="dropdown-toggle" data-toggle="dropdown">My Stocklists<b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li ng-repeat="stocklist in stocklists">
              <a href="<%= url_for(:action => 'index', :anchor => '/stocklist/') %>{{stocklist.id}}">{{stocklist.name}}</a>
            </li>
          </ul>
       </li>
      </ul>
      <div class="nav pull-right" ng-show="currentUser == null"><a class="btn" href="<%= url_for(:action => 'index', :anchor => '/login') %>" >Sign In</a></div>
      <div class="nav pull-right" ng-show="currentUser != null"><button class="btn" ng-click="logout()" >Logout {{currentUser.name}}</button></div>
    </div>
  </div>
</div>
<div class="container-fluid">
  <div class="row-fluid">
    <div class="span12">
      <%= yield %>
    </div>
  </div>
  <hr>
  <div>Horne Technology 2013</div>
</div> 
</body>
</html>
